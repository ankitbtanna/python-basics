# Annotations

def increment(number: int) -> int:
    """ This function increments the number
    Args:
        number (int): Number to be incremented

    Returns:
        number (int): The incremented number
    """
    return number + 1

print(increment(1))

print(help(increment))

increment("abc")
